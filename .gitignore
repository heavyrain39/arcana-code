# Dependencies
# 'npm install'로 설치되는 모든 패키지를 무시합니다.
# 용량이 매우 크고, package.json만 있으면 언제든 다시 설치할 수 있습니다.
/node_modules

# Build output
# 'npm run build' 실행 시 생성되는 배포용 폴더와 실행 파일을 무시합니다.
# 이들은 소스 코드가 아니며, 소스 코드로부터 언제든 재생성할 수 있습니다.
/dist
/ArcanaCode-v1.0.1
*.exe
*.txt

# Sensitive Information
# Google Gemini API 키가 들어있는 파일입니다. 절대로 Git에 올리면 안 됩니다.
key.txt

# Log files
# 프로그램 실행 중 생성될 수 있는 로그 파일을 무시합니다.
*.log
npm-debug.log*

# OS-generated files
# macOS와 Windows에서 자동으로 생성하는 시스템 파일을 무시합니다.
.DS_Store
Thumbs.db